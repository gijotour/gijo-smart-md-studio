# Dependencies
node_modules/

# OS / Temporary files
.DS_Store
Thumbs.db
*.log

# electron-builder output (NOT build/ — that holds committed source icons
# electron-builder reads from, per its own convention)
dist/
out/
